interface HeaderStringType {
    subMenu: {
        signin: string;
        signup: string;
    }
}

const text: HeaderStringType = {
    subMenu: {
        signin: "로그인",
        signup: "회원가입",
    }
};

export default text;